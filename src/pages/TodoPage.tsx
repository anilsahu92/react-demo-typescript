import React from "react";
import Todo from "../components/Todo";

interface Props {}

const TodoPage = (props: Props): JSX.Element => {
  //const {} = props;

  return <Todo />;
};

export default TodoPage;
